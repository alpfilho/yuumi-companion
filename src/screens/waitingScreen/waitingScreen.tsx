import React, { FC } from "react";

import "./waitingScreen.css";

export const WaitingScreen: FC = () => {
  return (
    <div className="waiting-screen">
      <h1 className="head1">Aguardando o jogo iniciar</h1>
    </div>
  );
};
